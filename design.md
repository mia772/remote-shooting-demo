# Professional Camera Companion App — Remote Shooting Demo

> UX/UI Design Specification · V1.0  
> 本规范用于指导可操作的 Remote Shooting Demo 设计与实现。产品需求优先级最高；现有 3 张设计图作为结构与交互基础；Hasselblad Phocus 仅作为专业视觉语言参考，不复制其具体界面。

## 1. Product Definition

### 1.1 产品定位

这是一个面向专业摄影工作流的相机 Companion App。用户连接相机后，在移动设备上完成实时取景、曝光参数调整、状态确认以及远程拍照或录像。

当前 Demo 只实现 **Remote Shooting**，不扩展社交、个人中心、相册社区、照片编辑、复杂账户体系或其他无关功能。

核心任务链路：

**连接相机 → 查看实时取景 → 调整参数 → 确认相机状态 → 拍照 / 录像**

### 1.2 Demo 成功标准

- 用户进入页面后能立即识别实时取景为核心区域。
- 用户能在 1 秒内扫读 M/A、ISO、EV、SHUTTER、WB。
- 用户能明确区分相机自动控制参数与可手动调整参数。
- 调参过程中尽可能保留实时取景、焦点标识与画面反馈。
- 红色快门按钮始终是最明确的主要操作。
- 页面在相机连接、参数写入、拍摄与录像状态变化时提供明确反馈。

### 1.3 目标用户

- 使用专业或高端相机进行人像、街拍、棚拍和静物拍摄的摄影师。
- 需要在机位之外构图、调参和触发快门的摄影助理或内容创作者。
- 熟悉 ISO、EV、快门、WB、M/A 模式，不需要消费级教学提示的用户。

### 1.4 核心场景

- 相机固定在脚架上，用户通过手机确认构图和对焦后远程拍照。
- 人像或街景拍摄中，用户边观察实时曝光变化边调整 EV。
- 用户在 M 模式下快速调整 ISO、快门和 WB。
- 用户切换到 A 模式，确认 ISO 与快门由相机自动控制，仅调整 EV 补偿。
- 用户设置自拍定时器或九宫格辅助后继续拍摄。
- 用户选择视觉滤镜，同时保留实时取景上下文。

## 2. Visual Direction

整体视觉参考 Hasselblad Phocus 的专业摄影工具气质，包括深色表面、数字层级、低噪声控件、克制留白和高信息效率；不得复制其页面结构、图标组合或具体交互布局。

### 2.1 视觉关键词

- 专业摄影工具
- 深色、极简、克制
- 低反射、低噪声
- 高信息密度但不拥挤
- 精确、稳定、仪器化
- 数字优先、标签次级

### 2.2 现有设计的保留与优化

保留：

- 顶部黑色状态区、中部实时取景、底部黑色控制区的纵向结构。
- 实时取景中的人像/街景画面、1x 标识和对焦标识。
- 居中的 M/A 模式标记。
- ISO、EV、快门、WB 四参数横向排列。
- 中央红色圆形快门按钮，底部左侧滤镜、右侧更多。
- EV 调节以窄条面板展开，取景画面持续可见。
- A 模式使用 A 前缀表达自动控制。

优化：

- 将 M/A 标记与参数区建立更明确的垂直关系，使其成为相机模式读数，而不是孤立悬浮标签。
- 固定四个参数单元的宽度和基线，避免数值变化造成跳动。
- 自动参数不仅降低亮度，还增加 A 前缀与不可编辑状态，避免仅靠颜色传达。
- 减少不必要的卡片边界和圆角，以分区、细分隔线和字重建立层级。
- 将所有参数浮层限制在画面下缘或底部控制区，避免遮挡主体与对焦点。

## 3. Design Principles

### 3.1 Live View First

实时取景是页面最大的连续视觉区域。任何参数面板都不得完全替代实时画面；调节 EV、ISO、WB 和滤镜时，应至少保留画面主体区域、1x 与对焦标识。

### 3.2 快速扫读

模式和四项参数位置固定。参数名称使用短标签，数值使用更大字号和等宽数字。参数从左到右固定为：

**ISO ｜ EV ｜ SHUTTER（界面可显示“快门”）｜ WB**

### 3.3 自动与手动清晰区分

- 手动可调：高对比白色；选中或编辑时为 `#E1011A`。
- 自动控制：A 前缀 + 降低亮度 + 禁用交互语义。
- 可调补偿：即使位于 A 模式，也保持可点击和较高对比度。

### 3.4 即时反馈，确认后提交

触控反馈应在 100ms 内出现，但参数只有在相机确认后才进入已提交状态。界面需要区分：

- 用户正在选择的预览值
- 正在发送的待确认值
- 相机最后确认值
- 写入失败后的恢复值

### 3.5 克制的强调色

`#E1011A` 只用于当前选中、关键参数高亮、快门、录像状态和重要错误反馈。中性信息、装饰与普通图标不得滥用红色。

### 3.6 单手可操作

高频操作集中在屏幕下半部；图标触控目标不小于 44×44px。核心拍摄动作无需跨越多个层级。

## 4. Live View Layout

以 390×844px 手机画板作为首要设计基准，同时适配 375px 小屏。

### 4.1 页面纵向结构

1. 系统安全区与状态栏
2. 相机状态栏
3. 实时取景区
4. 模式与参数区
5. 拍摄控制区
6. 底部安全区

### 4.2 顶部状态

- 时间固定展示为 `12:13`。
- 相机电量展示为 `89%` 并配电池图标。
- 左侧为关闭/返回图标。
- 右侧可保留省略号作为顶部上下文入口；当前 Demo 的功能性 More 入口仍以底部右侧为准。
- 图标触控区至少 44×44px，图标视觉尺寸 20–24px。
- 背景使用纯黑或近黑，不使用阴影卡片。

### 4.3 实时取景区

- 占据页面最大的视觉面积，建议高度 300–360px，按素材比例动态裁切。
- 图片保持正确比例，禁止拉伸。
- `1x` 使用半透明深色圆形底，位于取景区下部且避开主体。
- 对焦标识使用 1–1.5px 线框方形或括号式焦点框。
- 九宫格开启时使用 1px、20%–28% 白色透明度，不与焦点标识竞争。
- 离线时冻结最后一帧并覆盖低透明度黑层，不用插画占位。

### 4.4 参数面板打开时

- Live View 不重新排版、不闪跳。
- EV 等窄条面板优先贴近画面下缘展开。
- WB、ISO、Filter 使用底部面板时，面板最高不超过屏幕约 42%，并允许点击画面区域收起。
- 对焦标识与 1x 持续可见；若与面板重叠，则平移到剩余可见画面内。

## 5. M Mode

M 为 Demo 默认模式。

### 5.1 展示

- 模式标记：`M`
- 建议 28–32px 方形标记，圆角 4px。
- 白色底、黑色字，或等效的高对比仪器读数风格。

参数示例：

- `ISO 400`
- `EV -2`
- `快门 1/100s`
- `AWB`

### 5.2 交互

- ISO：可调整。
- EV：可调整。
- SHUTTER：可调整；Demo 首版可先展示确认态数值，并预留同类参数面板。
- WB：可选择。
- 当前正在调整的参数值和对应指示器使用 `#E1011A`。
- 未选中参数保持白色主值与灰色标签。

### 5.3 参数单元规则

- 每个单元宽度固定，数值居中或按统一基线对齐。
- 参数标签位于数值上方，或以一致的短标签形式与数值组合。
- 数字使用 tabular numbers，切换 `400 → 1600` 时不引起横向跳动。
- 整个参数单元均可点击，不要求用户精确点击文字。

## 6. A Mode

A 模式中，相机自动控制曝光参数。界面必须直接表达哪些值是相机决定的，哪些仍允许用户操作。

### 6.1 展示

- 模式标记：`A`
- ISO：`A400`
- EV：`A-2`
- SHUTTER：`A1/100s`
- WB：`WB A`

### 6.2 控制权限

| 参数 | 展示 | 用户权限 | 视觉状态 |
|---|---|---|---|
| ISO | A400 | 不可手动调整 | A 前缀、低亮度、禁用 |
| EV | A-2 | 可设置曝光补偿 | 高对比；选中时红色 |
| SHUTTER | A1/100s | 不可手动调整 | A 前缀、低亮度、禁用 |
| WB | WB A | 显示自动白平衡 | 自动状态；按产品支持决定是否允许切换 |

当前 Demo 按需求至少保证 EV 可调整；ISO 与 SHUTTER 不允许直接修改。

### 6.3 禁用参数反馈

点击 `A400` 或 `A1/100s` 时不打开编辑器，显示短提示：

- `A 模式下 ISO 由相机自动控制`
- `A 模式下快门由相机自动控制`

提示停留约 2 秒，不遮挡焦点和主体。

### 6.4 模式切换

M → A：

1. 模式标记进入待确认态。
2. ISO 与快门立即显示 A 前缀并暂时降低亮度。
3. 相机确认后更新实际自动值。
4. 失败则回退到 M，并说明未切换原因。

A → M：

1. 请求相机返回最新可用参数。
2. 确认后移除 ISO 与快门的 A 前缀。
3. 恢复对应参数的可点击状态。
4. 不沿用未经确认的历史手动值。

## 7. EV Interaction

### 7.1 入口与布局

点击 EV 参数后，在实时取景下缘与参数行之间打开窄条曝光控制面板，延续现有设计图的交互基础。

- 选项：`-2`、`-1`、`0`、`+1`、`+2`
- 滑块范围：`[-2, +2]`
- 当前值：`0.0`
- 当前值颜色：`#E1011A`
- 当前值下方使用小型红色三角指针与刻度关联。

### 7.2 行为

- 点击刻度：直接跳到对应整数值。
- 拖动：连续调整；建议步进 0.3EV 或设备实际支持步进。
- 拖动期间实时取景持续显示曝光变化。
- 松手后向相机发送最终值。
- 相机确认后写入参数行。
- 点击画面或再次点击 EV 收起面板。

### 7.3 状态

- 预览中：数值为红色，参数行显示临时值。
- 待确认：数值旁显示 2–3px 脉冲点或细小进度环。
- 已确认：值稳定显示。
- 失败：回到上一确认值，提示 `EV 调整失败，相机未接受该值`。

### 7.4 视觉约束

- 面板是曝光刻度带，不是普通设置卡片。
- 不使用大标题、渐变、厚阴影或大圆角容器。
- 非当前刻度为次级灰；刻度线保持细而清晰。
- 取景、1x 和焦点标识必须保留。

## 8. ISO Interaction

### 8.1 选项

`自动`、`200`、`400`、`800`、`1600`、`3200`、`6400`

### 8.2 M 模式

- 点击 ISO 打开横向数值轨道或紧凑底部面板。
- 当前值使用 `#E1011A` 和选中指示器。
- 所有数字使用等宽数字。
- 选中后先显示待确认态，再以相机返回值为准。
- `自动` 为合法选项，但与数字项使用明确文本区分。

### 8.3 A 模式

- 显示 `A400` 等实时自动值。
- 参数不可编辑，不打开 ISO 选择面板。
- 点击后显示 `A 模式下 ISO 由相机自动控制`。
- 禁用状态仍需满足文本对比度；不得淡到无法读取。

## 9. WB Interaction

### 9.1 选项

- 自动白平衡
- 晴天
- 阴天
- 阴影
- 人造光
- 闪光灯
- 色温

### 9.2 面板

- 手机端使用底部横向选项轨道或紧凑列表。
- 当前选项使用 `#E1011A` 的文字、描边或选中标记。
- 非当前项使用中性灰，不采用彩色天气插画。
- 图标可使用细线摄影语义图标；图标下保留短标签。

### 9.3 行为

- 选择预设时，实时取景立即预览白平衡变化。
- 相机确认后更新参数行。
- 选择“色温”后进入第二级 Kelvin 控件，例如 `5500K`。
- Kelvin 使用精确滑杆与明确数值，滑动时不隐藏取景画面。

### 9.4 自动白平衡

- M 模式下选择自动白平衡时可显示 `AWB`。
- A 模式需求展示为 `WB A`。
- 两者语义均需在无障碍标签中读为“自动白平衡”，避免逐字母造成理解负担。

## 10. Filter Interaction

### 10.1 入口

底部左侧 Filter 图标，触控区域 44×44px，视觉图标约 24px。

### 10.2 选项

- Greg Williams
- Bleach
- Teal
- Eternal
- Contemporary
- Natural
- Cine

### 10.3 面板与行为

- 从底部或左侧打开低噪声选择面板。
- 尽量保留实时取景，不使用全屏页面。
- 使用专业预设列表：名称、可选短代码、当前选中标记。
- 当前选项使用 `#E1011A`，同时提供勾选或边线，不能只靠颜色。
- 点击选项后实时预览。
- 首版不使用大型滤镜预览卡片，避免压缩取景空间和产生消费级视觉。
- 若滤镜仅影响 App 预览、不写入相机文件，必须在 Demo 中标记为“仅预览”。

## 11. More Menu

底部右侧 More 图标打开紧凑深色菜单。当前只包含以下两项，不增加其他功能。

### 11.1 一级菜单

- 自动定时器 ›
- 九宫格辅助 ›

使用行式结构、细分隔线和右侧箭头，不使用独立卡片。

### 11.2 自动定时器二级

- 3 秒
- 10 秒

当前选择使用 `#E1011A` 与勾选标记。选择后，快门下方“自拍定时器”显示当前状态；触发拍摄时提供清晰倒计时。

### 11.3 九宫格辅助二级

- 开
- 关

“开”时在实时取景叠加 3×3 细线网格；“关”时移除。网格不改变照片内容，只辅助构图。

## 12. Bottom Shooting Controls

### 12.1 快门

- 位于底部控制区水平中心。
- 直径 64–72px。
- 填充 `#E1011A`。
- 可使用低透明白色或深灰外环，但不得增加发光、渐变或厚重阴影。
- 按下状态缩放至 0.96 或轻微加深，不能造成布局位移。

### 12.2 模式标签

快门下方显示：

**自拍定时器 ｜ 照片 ｜ 视频**

- 默认选中“照片”。
- 选中状态使用高对比白色或细红色下划线。
- 避免三个标签全部使用红色，以免与快门竞争。
- 切换“视频”后，中央主按钮必须显示明确的录像语义。

### 12.3 左右入口

- 左：Filter 图标，圆形深灰按钮。
- 右：More 图标，圆形深灰按钮。
- 两者视觉权重低于中央快门，但触控尺寸一致满足 44×44px。

### 12.4 拍摄与录像

拍照：

- 点击快门后显示短暂取景闪白或红色外环脉冲。
- 不使用庆祝动画。
- 相机未确认时防止重复触发。

录像：

- 开始录像后，红色圆形按钮切换为清晰的停止形态，例如红色圆底内白色方形。
- 显示红色录制指示与持续时间。
- 录制与停止必须在形状、文字和无障碍标签上均可区分。

## 13. Typography

### 13.1 字体

- iOS/macOS：SF Pro
- Web Demo：`Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`
- 数字统一启用 tabular numbers。

### 13.2 字级

| 用途 | 字号 | 字重 | 说明 |
|---|---:|---:|---|
| 系统时间 | 15–17px | 600 | 遵循系统状态栏 |
| 相机电量 | 12–14px | 500 | 与电池图标成组 |
| M/A 模式 | 20–24px | 700 | 最强模式标识 |
| 参数标签 | 11–12px | 500 | 次级灰 |
| 参数数值 | 21–24px | 500–600 | 等宽数字 |
| EV 当前值 | 22–24px | 600 | 红色 |
| EV 刻度 | 15–16px | 500 | 非当前为次级灰 |
| 底部标签 | 10–12px | 500 | 不低于可读下限 |
| 面板标题 | 20–24px | 600 | 克制，不做大标题 |
| 面板行文字 | 16–20px | 500 | 根据设备宽度调整 |

### 13.3 排版规则

- 参数数值必须大于参数标签。
- 不使用装饰性字体。
- 不使用负字距压缩信息。
- 英文滤镜名允许换行一次，但不截断关键名称。
- 中文“快门”与英文参数缩写保持一致基线。

## 14. Color Tokens

### 14.1 核心色

| Token | Value | 用途 |
|---|---|---|
| `color.primary` | `#E1011A` | 快门、当前选中、活动参数、录像、重要错误 |
| `color.bg` | `#000000` | 页面主背景 |
| `color.bgSoft` | `#080808` | 次级背景 |
| `color.panel` | `#181818` | 参数与菜单面板 |
| `color.panelRaised` | `#202020` | 需要轻微区分的表面 |
| `color.divider` | `#333333` | 分隔线 |
| `color.hairline` | `rgba(255,255,255,0.12)` | 细边线 |

### 14.2 文本色

| Token | Value | 用途 |
|---|---|---|
| `text.primary` | `#FFFFFF` | 主要读数 |
| `text.secondary` | `rgba(255,255,255,0.68)` | 标签和说明 |
| `text.muted` | `rgba(255,255,255,0.42)` | 非重点信息 |
| `text.disabled` | `rgba(255,255,255,0.34)` | 禁用参数，仍需可读 |

### 14.3 状态色

| Token | Value | 用途 |
|---|---|---|
| `status.online` | `#7BD88F` | 在线状态 |
| `status.warning` | `#F2B84B` | 电量或空间预警 |
| `status.error` | `#E1011A` | 离线、失败 |
| `status.recording` | `#E1011A` | 录像状态 |

约束：

- 页面整体应保持黑与中性灰占主导。
- 不沿用 Phocus 截图中的橙色强调，统一使用需求指定的 `#E1011A`。
- 主表面不使用渐变。
- 红色不能用于普通标签和非选中装饰。

## 15. Spacing

采用 4px 基础网格。

| Token | Value |
|---|---:|
| `space.1` | 4px |
| `space.2` | 8px |
| `space.3` | 12px |
| `space.4` | 16px |
| `space.5` | 20px |
| `space.6` | 24px |
| `space.8` | 32px |

### 15.1 页面尺寸建议

- 顶部状态区域：含安全区共 88–112px。
- 实时取景高度：300–360px，最终由素材比例决定。
- 取景区到 M/A 标记：48–72px，允许随屏幕高度压缩。
- 参数行高度：56–72px。
- 快门控制区：96–120px。
- EV 条高度：58–68px。
- 菜单行高度：56–68px。
- 图标触控目标：最小 44×44px。

### 15.2 圆角

- 实时取景：0px；除非画面容器需要裁切。
- 参数选中带：0–4px。
- M/A 标记：4px。
- 图标按钮与快门：圆形。
- 底部面板：仅顶部使用 16–20px 圆角。
- 菜单行：不做卡片圆角，以分隔线区分。

## 16. Component Rules

### 16.1 Parameter Cell

- 结构：短标签 + 主数值。
- 最小高度 44px，整个单元可点击。
- 默认值为白色，标签为次级灰。
- 选中值为红色，并增加指示线或标记。
- 自动禁用值带 A 前缀、降低亮度并设置禁用语义。
- 参数变化时保持固定宽度。

### 16.2 Mode Readout

- 仅显示 M 或 A。
- 视觉权重高于参数标签。
- 若支持点击切换，应打开极简 M/A 选择，不引入转盘等额外复杂交互。
- 切换时必须表现待确认与失败回退。

### 16.3 Icon Button

- 触控区域至少 44×44px。
- 使用同一套矢量线性图标。
- 线宽建议 1.5–2px。
- 每个仅图标按钮必须有无障碍名称和按下状态。
- 不使用 Emoji 或带不同视觉风格的混合图标。

### 16.4 Parameter Panel

- 由深色平面、刻度/列表和细分隔线组成。
- 不使用大面积卡片嵌套。
- 面板打开后维持取景区域稳定。
- 选中、待确认和错误状态使用统一规则。

### 16.5 Status Indicator

- 在线：小型状态点 + 文本语义，不能仅显示绿点。
- 电量：百分比 + 电池图标。
- SD 卡空间若在后续状态栏加入，应使用剩余容量读数，不新增仪表卡片。
- 状态信息不得抢占取景主体。

## 17. Interaction States

### 17.1 Default

- 主值白色、标签灰色、背景黑色。
- 控件位置固定，无不必要动画。

### 17.2 Selected

- 当前值、图标或刻度使用 `#E1011A`。
- 同时提供下划线、勾选、描边或位置指示，不能只靠颜色。

### 17.3 Pressed

- 80–100ms 内提供反馈。
- 使用轻微缩放、透明度或背景变化。
- 不引发布局位移。

### 17.4 Pending

- 数值旁出现细小进度环或脉冲点。
- 保留上一相机确认值用于失败恢复。
- 不阻塞实时取景。

### 17.5 Loading / Connecting

- 连接相机时保留页面骨架。
- 取景区使用低对比加载指示，不使用大型品牌动画。
- 文案：`正在连接相机…`
- 超时后提供明确的重试入口。

### 17.6 Offline

- 冻结或轻微压暗最后一帧。
- 显示 `相机离线` 与 `重新连接`。
- 禁用快门、录像和参数写入。
- 关闭与 More 入口仍可使用。

### 17.7 Error

错误信息具体说明对象和恢复方式：

- `相机离线，重新连接后才能拍摄`
- `A 模式下 ISO 由相机自动控制`
- `EV 调整失败，已恢复为 0.0`
- `拍摄失败，请检查相机与 SD 卡状态`

### 17.8 Success

- 拍照：短暂快门闪白或红色外环脉冲。
- 参数提交：待确认标记消失即可，不额外弹出成功 Toast。
- 录像开始：录制指示、计时和停止形态持续可见。

### 17.9 Accessibility

- 正文和关键数值满足 WCAG AA 对比度；禁用状态仍应可辨认。
- 触控目标最小 44×44px，目标间保留至少 8px。
- M/A、自动/手动、选中/未选中、录像/停止不能只依靠颜色。
- 焦点顺序遵循：顶部状态 → 取景辅助 → 模式 → 参数 → 拍摄控制 → 底部入口。
- 屏幕阅读器应读出完整语义，例如“ISO，自动，当前 400，不可调整”。
- 滑块提供当前值、最小值、最大值和增减操作。
- 支持系统文字放大时，参数数值不得相互覆盖。
- 遵循 Reduced Motion；关闭缩放与脉冲动画后仍有静态反馈。
- 所有动态状态通过适当的 live region 或原生可访问性通知播报，但避免重复播报实时参数刷新。

## 18. Do / Don't

### Do

- 延续现有 3 张设计图的主页面结构与控件关系。
- 让实时取景成为最大、最连续的视觉区域。
- 用固定位置、等宽数字和清晰字重提高参数扫读速度。
- 在 A 模式用 A 前缀、亮度和交互状态共同表达自动控制。
- EV 调整期间保留取景、焦点与 1x。
- 让快门成为最明确的主操作。
- 使用窄参数带、行式列表、细分隔线和克制圆角。
- 保持 Filter 和 More 的层级浅、内容严格受限。

### Don't

- 不复制 Hasselblad Phocus 的具体布局。
- 不加入作品集说明、设计过程展示或营销文案。
- 不扩展社交、社区、个人中心、编辑、账号等功能。
- 不使用消费级大卡片、渐变、玻璃拟态、发光或大量圆角。
- 不让浮层遮住取景主体和焦点。
- 不滥用红色。
- 不允许 A 模式下直接编辑 ISO 与 SHUTTER。
- 不用纯颜色区分状态。
- 不使用 Emoji 图标。
- 不为 More 菜单添加定时器和九宫格以外的功能。

## 19. Demo Implementation Notes

### 19.1 首版实现范围

1. 实现单一 Remote Shooting 主页面。
2. 以现有 M 模式设计作为默认状态。
3. 实现 EV 窄条调节面板和实时曝光预览。
4. 实现 A 模式及 `A400`、`A-2`、`A1/100s`、`WB A`。
5. 实现 ISO 与 WB 选择面板。
6. 实现 Filter 列表。
7. 实现 More → 自动定时器 / 九宫格辅助。
8. 实现拍照、录像、连接、离线、失败与待确认状态。

### 19.2 React / Next.js 组件边界建议

- `RemoteShootingDemo`：页面状态与相机模拟层。
- `TopCameraBar`：关闭、时间、相机电量与顶部入口。
- `LiveView`：画面、焦点、1x、网格与离线覆盖层。
- `ModeReadout`：M/A 展示与切换。
- `ParameterRow`：ISO、EV、SHUTTER、WB。
- `EVControlStrip`：-2 至 +2 刻度、滑块和待确认状态。
- `ISOControlPanel`：ISO 选项与 A 模式禁用反馈。
- `WBControlPanel`：WB 预设与 Kelvin 二级控制。
- `FilterPanel`：七种滤镜及当前选择。
- `MoreMenu`：定时器和九宫格的两级菜单。
- `ShootingControls`：Filter、快门、自拍/照片/视频、More。

以上为实现边界，不代表需要为本规范创建代码。

### 19.3 状态模型建议

- `mode: M | A`
- `iso: Auto | 200 | 400 | 800 | 1600 | 3200 | 6400`
- `ev: -2 ... +2`
- `shutter: 1/100s` 等相机返回值
- `wb: 自动白平衡 | 晴天 | 阴天 | 阴影 | 人造光 | 闪光灯 | 色温`
- `filter: Greg Williams | Bleach | Teal | Eternal | Contemporary | Natural | Cine`
- `timer: none | 3s | 10s`
- `grid: on | off`
- `captureMode: photo | video`
- `cameraStatus: connecting | online | offline | error`
- `recording: boolean`
- 每个相机参数额外保存 `previewValue`、`pendingValue`、`confirmedValue`。

### 19.4 技术与交互约束

- 使用语义化设计 Token，不在组件中散落硬编码颜色。
- 参数与倒计时启用 tabular numbers。
- 实时取景容器尺寸固定，加载和面板切换时避免布局偏移。
- 图标来自同一套 SVG 图标系统，并提供 accessible name。
- 顶部和底部使用 safe-area inset。
- 面板与按压过渡控制在 120–180ms。
- 尊重 Reduced Motion。
- 首要测试尺寸：390×844px；补测 375px 宽度。
- Demo 使用模拟数据时，需保持相机确认延迟和失败状态可测试。

### 19.5 视觉验收标准

- 第一眼明确是专业相机远程控制界面，而非消费级拍照 App。
- 实时取景在面积和视觉权重上占绝对主导。
- M/A 状态无需解释即可识别。
- A 模式自动参数与可调 EV 明显不同。
- 参数数值在不同长度下不跳动、不挤压。
- EV、ISO、WB、Filter 面板打开后仍能观察取景反馈。
- 快门按钮是页面唯一最强主操作。
- Filter 与 More 呈现为专业工具面板，不呈现为内容卡片。
